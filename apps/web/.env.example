# Base URL of the genzh control-plane API.
# The media server URL is not configured here: the API returns it per room in
# the media-join response, so a client never needs to know it in advance.
VITE_API_URL=http://127.0.0.1:8080
